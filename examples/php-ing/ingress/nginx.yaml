apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nm-php-apache-ing-nginx
  annotations:
    kubernetes.io/ingress.class: nginx
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /test
            backend:
              serviceName: php-svc
              servicePort: 80
